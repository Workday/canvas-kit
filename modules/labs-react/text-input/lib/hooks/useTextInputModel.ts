import {
  useFormFieldModel,
  FormFieldModel,
  FormFieldModelConfig,
} from '@workday/canvas-kit-labs-react/form-field';

export const useTextInputModel = useFormFieldModel;

export type TextInputModel = FormFieldModel;

export type TextInputModelConfig = FormFieldModelConfig;
