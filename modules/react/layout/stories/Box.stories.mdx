import {Meta} from '@storybook/addon-docs';
import {Box} from '@workday/canvas-kit-react/layout';
// examples
import {Basic} from './examples/Box/Basic';

<Meta title="Components/Containers/Box" component={Box} />

# Box

`Box` is a primitive component that provides a common, ergonomic API around Canvas design tokens. It
is highly flexible, and its primary purpose is to build other components.

## Installation

```sh
yarn add @workday/canvas-kit-react
```

## Usage

### Basic Example

<ExampleCodeBlock code={Basic} />

## Props

### The `As` Prop

The `as` prop allows you to override the underlying element of a component or combine it with
another component. By default, `Box` will render a `div` element, but sometimes that's not what you
want. Below, we have a `Box` that we want to render as a semantic anchor element. The most
noticeable transformation is that `Box` renders as a link. But also notice that while it still
supports all `BoxProps` such as `padding`, it also supports semantic anchor element props such as
`href`.

### Style Props

`Box` exposes [style props](/docs/features-style-props--background-example#style-props) that allow
you to modify styles in an ergonomic way across components. To learn more about individual sets of
`Box` style props, consult the lists below.

- [Background Style Props](/docs/features-style-props--background-example)
- [Border Style Props](/docs/features-style-props--border-example)
- [Color Style Props](/docs/features-style-props--color-example)
- [Depth Style Props](/docs/features-style-props--depth-example)
- [Flex Item Style Props](/docs/features-style-props--flex-item-example)
- [Grid Item Style Props](/docs/features-style-props--grid-item-example)
- [Layout Style Props](/docs/features-style-props--layout-example)
- [Other Style Props](/docs/features-style-props--other-example)
- [Position Style Props](/docs/features-style-props--position-example)
- [Space Style Props](/docs/features-style-props--space-example)
- [Text Style Props](/docs/features-style-props--text-example)
