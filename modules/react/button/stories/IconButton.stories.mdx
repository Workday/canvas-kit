import {Meta, Story} from '@storybook/addon-docs/blocks';

import {IconButton} from '@workday/canvas-kit-react/button';
import {Specifications} from '@workday/canvas-kit-docs';

import {Circle} from './examples/Icon/Circle';
import {CircleFilled} from './examples/Icon/CircleFilled';
import {Inverse} from './examples/Icon/Inverse';
import {InverseFilled} from './examples/Icon/InverseFilled';
import {Plain} from './examples/Icon/Plain';
import {Square} from './examples/Icon/Square';
import {SquareFilled} from './examples/Icon/SquareFilled';
import {Toggleable} from './examples/Icon/Toggleable';

<Meta title="Components/Buttons/Button/React/Icon Button" component={IconButton} />

# Canvas Kit Icon Button

Clickable icon button elements that extend the native `<button>` element with Canvas styling.

[> Workday Design Reference](https://design.workday.com/components/buttons/icon-buttons)

## Installation

```sh
yarn add @workday/canvas-kit-react
```

## Usage

Here is a basic example of a Primary Button. Buttons can be customized with several props to change
the size, add an icon, etc.

<ExampleCodeBlock code={Circle} />

### Variants

`circle` is the default variant. Below are the other available variants:

#### CircleFilled

<ExampleCodeBlock code={CircleFilled} />

#### Plain

<ExampleCodeBlock code={Plain} />

#### Square

<ExampleCodeBlock code={Square} />

#### SquareFilled

<ExampleCodeBlock code={SquareFilled} />

#### Inverse

<ExampleCodeBlock code={Inverse} />

#### InverseFilled

<ExampleCodeBlock code={InverseFilled} />

### Toggleability

The `toggle` prop can be used to make icon buttons toggleable.

<ExampleCodeBlock code={Toggleable} />

## Properties

<ArgsTable of={IconButton} />
