import {Meta} from '@storybook/addon-docs';
import {Specifications} from '@workday/canvas-kit-docs';
import {CompoundDisclosureExample} from './examples/CompoundDisclosureExample';

<Meta title="Hooks and Utilities" />

# Compound Disclosure Example

This example showcases the usage of our utility functions to compose and create a compound component
using `createModelHook`, `createContainer`, `createSubcomponent` and `createElemPropsHook`.

### Usage of Utilify Functions Together

Together you can create a powerful compound component

<ExampleCodeBlock code={CompoundDisclosureExample} />
