import {Meta, Story} from '@storybook/addon-docs';
import {Density} from './examples/Density';

<Meta title="Examples/Forms/Density" />

# Canvas Kit Examples

## Density

It's common to adjust density within a form based on visual preferences. While Canvas Kit doesn't
currently support a density system, we still allow our components to be flexibile enough through
styling to achieve this.

<ExampleCodeBlock code={Density} />
