Canvas Kit 14.0 Upgrade Guide - LLM Support Reference

================================================================================
OVERVIEW
================================================================================

This is Canvas Kit version 14.0 upgrade guide for LLM assistance. It contains breaking changes, deprecations, removals, and migration instructions for upgrading from v13 to v14.

Key areas of change:
- Caution naming (Alert → Caution)
- Theming changes (Canvas Provider)
- Component visual updates (brand refresh)
- Component API changes
- Deprecations and removals

================================================================================
CODEMOD AVAILABILITY
================================================================================

A codemod is available to automate most breaking changes:
- Command: npx @workday/canvas-kit-codemod v14 [path]
- Handles changes marked with 🤖
- Requires manual review and QA
- Only works on .js, .jsx, .ts, .tsx files

================================================================================
BREAKING CHANGES
================================================================================

1. CAUTION NAMING
   - Alert/Warning states renamed to Caution
   - Changes affect: ErrorType.Alert → ErrorType.Caution
   - Changes affect: error="alert" → error="caution"
   - Impacts: FormField, TextInput, Switch, TextArea components
   - Codemod handles: YES

2. CANVAS PROVIDER (MAJOR BREAKING CHANGE)
   - Removed default branding color generation
   - Changed from chroma.js to oklch color generation
   - Theme prop updated to include 'lighter' property
   - May cause color discrepancies
   - Required: Provide full palette or use createCanvasTheme for chroma.js behavior

3. CARD COMPONENT
   - Default shadow removed (visual breaking change)
   - New variants: borderless, filled
   - Gap replaces individual margins (visual breaking change)
   - Uses display: flex with flex-direction: column

4. BUTTONS - TERTIARY BUTTON
   - Removed isThemable prop
   - Shape changed to rounded corners
   - Use cs prop or colors prop for customization

5. SEARCH FORM
   - SearchThemeAttributes: boxShadow and boxShadowFocus only accept string (not string array)
   - SearchTheme enum uses string values: 'light', 'dark', 'transparent'

6. STATUS INDICATOR (PREVIEW)
   - Variant name changes:
     * blue → primary
     * green → success  
     * orange → caution
     * red → critical
     * gray → neutral
   - StatusIndicatorVariant type deprecated

================================================================================
COMPONENT UPDATES (VISUAL/API CHANGES)
================================================================================

AVATAR (PREVIEW)
- New component in preview package
- Required name prop (replaces altText)
- New variant options: teal, purple, amber, blue
- Size options: extraExtraLarge, extraLarge, large, medium, small, extraSmall, extraExtraSmall
- BaseAvatar for button elements

BRANDING CHANGES
- Multiple components updated for brand refresh:
  Breadcrumbs, Buttons, ColorPicker, InformationHighlight, LoadingDots, 
  Pagination, Pill, SearchForm, SegmentedControl, ToolbarDropdownButton, ToolbarIconButton

COUNT BADGE
- New emphasis prop: 'high' or 'low'
- Visual updates for brand refresh

EXPANDABLE
- Hover state color updated
- Focus state color updated
- Avatar prop: altText → name (codemod handles)

HYPERLINK & EXTERNAL HYPERLINK
- New variants: 'standalone' and 'standalone-inverse'
- Removes underline for non-body text usage

LOADING DOTS
- Color changed from system.color.bg.alt.strong to system.color.bg.muted.strong
- New inverse variant for dark backgrounds

PILL
- Avatar prop: altText → name (codemod handles)
- Removed 'default' variant support
- readyOnlyPillStencil and removeablePillStencil removed

================================================================================
DEPRECATIONS
================================================================================

DEPRECATED COMPONENTS (will be removed in future versions):
1. Avatar (Main) → Use Avatar (Preview)
2. Combobox (Labs) → Use Combobox (React)
3. Radio (Main) → Use new Radio (same package)
4. SearchForm (Labs) → Use Combobox (React)
5. Segmented Control (Main) → Use Segmented Control (Preview)
6. Side Panel (Main) → Use Side Panel (Preview)
7. Tokens (canvas-kit-react/tokens) → Use @workday/canvas-tokens-web

================================================================================
REMOVALS
================================================================================

REMOVED COMPONENTS/UTILITIES:
1. DeprecatedButton - Use PrimaryButton, SecondaryButton, or TertiaryButton
2. InputProvider - No longer needed, use :focus-visible CSS
3. Menu (Preview) - Use Menu from Main package
4. readyOnlyPillStencil and removeablePillStencil - Use pillStencil
5. TextArea (Preview) - Use TextArea from Main package  
6. TextInput (Preview) - Use TextInput from Main package

================================================================================
MIGRATION EXAMPLES
================================================================================

CAUTION NAMING:
Before: <FormField error="alert">
After:  <FormField error="caution">

CANVAS PROVIDER:
Before: <CanvasProvider theme={{canvas: {palette: {primary: {main: 'purple'}}}}}>
After:  <CanvasProvider theme={{canvas: {palette: {primary: {main: cssVar(base.blue600)}}}}}>

CARD:
Before: <Card> (had shadow)
After:  <Card cs={{boxShadow: system.depth[1]}}> (for shadow)

AVATAR:
Before: <Avatar altText="John Doe" size="large" />
After:  <Avatar name="John Doe" size="large" variant="blue" />

STATUS INDICATOR:
Before: <StatusIndicator variant="blue" />
After:  <StatusIndicator variant="primary" />

PILL:
Before: <Pill variant="default"><Pill.Avatar altText="John Doe" /></Pill>
After:  <Pill><Pill.Avatar name="John Doe" /></Pill>

================================================================================
TROUBLESHOOTING
================================================================================

COMMON ISSUES:
1. Dependency conflicts - Update all Canvas Kit packages together
2. Color discrepancies - Due to chroma.js → oklch change
3. Visual changes - Brand refresh may affect appearance
4. Build errors - Run linter after codemod
5. Token issues - Ensure @workday/canvas-tokens-web is updated

DEPENDENCY VERSIONS:
- @workday/canvas-kit-react: ^13.5.6 → ^14.0.0
- @workday/canvas-kit-styling: ^13.5.6 → ^14.0.0  
- @workday/canvas-kit-preview-react: ^13.5.6 → ^14.0.0
- @workday/canvas-kit-labs-react: ^13.5.6 → ^14.0.0

================================================================================
PACKAGE TYPES
================================================================================

MAIN: Stable, production-ready components
PREVIEW: Mostly stable, may receive breaking changes before promotion to Main
LABS: Experimental, may change significantly or be removed

================================================================================
ADDITIONAL RESOURCES
================================================================================

- GitHub Issues: https://github.com/Workday/canvas-kit/issues/new
- Component Documentation: https://workday.github.io/canvas-kit/
- Canvas Tokens: https://workday.github.io/canvas-tokens/
- Migration Guide: https://workday.github.io/canvas-kit?path=/docs/guides-tokens-migration-overview--docs

================================================================================
END OF GUIDE
================================================================================