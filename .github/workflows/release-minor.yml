name: 'Minor Release'
on:
  workflow_dispatch:

jobs:
  release-minor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout master
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0
        with:
          persist-credentials: false
          fetch-depth: 0 # Needed to do a proper push
          ref: master # Release job only works on master

      - name: Pull minor changes
        run: git pull origin prerelease/minor

      - uses: Workday/canvas-kit-actions/install@38458d8f53521968d38ad2b839fe442fe3046c6a # v1
        with:
          node_version: 16.x

      - uses: Workday/canvas-kit-actions/release@38458d8f53521968d38ad2b839fe442fe3046c6a # v1
        with:
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          gh_rw_token: ${{ secrets.GH_RW_TOKEN }}
          publish_token: ${{ secrets.NPM_CI_PUBLISH_TOKEN }}
          chromatic_project_token: ${{ secrets.CHROMATIC_APP_CODE }}
          version: 'minor'

      - uses: Workday/canvas-kit-actions/report-failure@38458d8f53521968d38ad2b839fe442fe3046c6a # v1
        if: failure()
        with:
          slackWebhook: ${{ secrets.SLACK_WEBHOOK }}
          slackMessage: |
            Minor Release job failed. Please check error logs.
